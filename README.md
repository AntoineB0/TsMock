# TsMock
A web application that generates realistic mock data for developers. Define your fields, choose data types (names, dates, UUIDs, emails, etc.), and instantly export complete datasets in CSV, JSON, or SQL format.
